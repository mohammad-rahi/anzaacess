export type EventTypes = {
    id?: string;
    created_at?: string;
    profile_id: string;
    event_name: string;
    event_description: string;
    event_category: string;
    event_image: string;
    event_date_time: string;
    event_venue: string;
    ticket_types: string;
}