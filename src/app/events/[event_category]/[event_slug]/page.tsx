import React from 'react'

export default function EventDetailsPage({ params: { event_category, event_slug } }: { params: { event_category: string, event_slug: string } }) {
    return (
        <div>EventDetailsPage</div>
    )
}
